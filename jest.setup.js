/** Se ejecutará apenas inicialicen mis pruebas  */

// importamos el whatwg-fetch para los tests
import 'whatwg-fetch';

// Configuramos el dotenv para obtener las variables de entorno para las prubas
require('dotenv').config({
    path: '.env'
})